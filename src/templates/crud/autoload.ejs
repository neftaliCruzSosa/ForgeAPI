import express from 'express';
const router = express.Router();

<% entities.forEach(entity => { %>
import <%= entity %>Routes from './<%= entity %>.routes.js';
router.use('/<%= entity.toLowerCase() %>', <%= entity %>Routes);
<% }); %>

export default router;