# <%= config.projectName %>

Project generated automatically with **ForgeAPI** 🛠️

---

## 🧰 Technologies Used

- Node.js
- <%= presets.framework.label %>
- <%= presets.db.label %><%if (config.authType) {%>
- <%= presets.auth.label %><% } %>
- Dotenv

---

## 🚀 Getting Started

```bash
npm install
npm run dev
```

> The server will start at [http://localhost:3000](http://localhost:3000)

---

## ⚙️ Environment Configuration

Create a `.env` file based on the example:

```bash
cp .env.example .env
```

Edit the values based on your environment:

```env
<% presets.framework.env.forEach(line => { -%>
<%- line.key %>=<%- line.value %>
<% }) %><%config.authType && presets.auth.env.forEach(line => { -%>
<%- line.key %>=<%- line.value %>
<% }) %><% presets.db.env(config.projectName).forEach(line => { -%>
<%- line.key %>=<%- line.value %>
<% }) %>
```
<% if (config.authType) {%>
----

## 🔐 Auth Routes
<% presets.auth.routes.forEach(line => { -%>
- `<%- line.method %> <%- line.path %>` - <%- line.description %>
<% }) %>
---
<% } %>
## 🔁 Entity Endpoints<% config.entities.forEach(entity => { %>

### `/api/<%= entity.name.toLowerCase() %>`

- `POST /` → Create a new <%= entity.name %>
- `GET /` → Get all <%= entity.name %>'s
- `GET /:id` → Get a <%= entity.name.toLowerCase() %> by ID
- `PUT /:id` → Update a <%= entity.name.toLowerCase() %>
- `DELETE /:id` → Soft delete
- `PATCH /:id/restore` → Restore a deleted <%= entity.name.toLowerCase() %>
- `DELETE /:id/hard` → Hard delete (Permanent)<% }); %>

---

## 📂 Project Structure

```
.
├── app.js
├── .env
├── models/
├── routes/
├── controllers/
├── middlewares/
├── auth/
└── db.js
```

---

## 🧪 Scripts

```bash
npm run dev    # Development with nodemon
npm start      # Production
```

---

## 📚 README generated by the modular generator.
