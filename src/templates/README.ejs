# <%= projectName %>

Project generated automatically with **API Forge** 🛠️

---

## 🧰 Technologies Used

- Node.js
- Express
- <%= dbLabel %>
- JSON Web Tokens (JWT)
- Dotenv

---

## 🚀 Getting Started

```bash
npm install
npm run dev
```

> The server will start at [http://localhost:3000](http://localhost:3000)

---

## ⚙️ Environment Configuration

Create a `.env` file based on the example:

```bash
cp .env.example .env
```

Edit the values based on your environment:

```env
PORT=3000
JWT_SECRET=supersecret
JWT_EXPIRES_IN=7d
<% dbEnvVars.forEach(line => { -%>
<%- line %>
<% }) %>
```

---

## 🔐 Auth Routes

- `POST /auth/register`
- `POST /auth/login`

---

## 🔁 Entity Endpoints

<% entities.forEach(entity => { %>

### `/api/<%= entity.name.toLowerCase() %>`

- `POST /` → Create a new <%= entity.name %>
- `GET /` → Get all <%= entity.name %>'s
- `GET /:id` → Get a <%= entity.name.toLowerCase() %> by ID
- `PUT /:id` → Update a <%= entity.name.toLowerCase() %>
- `DELETE /:id` → Soft delete
- `PATCH /:id/restore` → Restore a deleted <%= entity.name.toLowerCase() %>
- `DELETE /:id/hard` → Hard delete (Permanent)
  <% }); %>

---

## 📂 Project Structure

```
.
├── app.js
├── .env
├── models/
├── routes/
├── controllers/
├── middlewares/
├── auth/
└── db.js
```

---

## 🧪 Scripts

```bash
npm run dev    # Development with nodemon
npm start      # Production
```

---

## 📚 README generated by the modular generator.
